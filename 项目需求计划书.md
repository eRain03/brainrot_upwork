#### 项目需求文档（PRD） - Eldorado AI 更新版
基于客户原描述、UI mockup 图片，以及最新更新（暂不集成 Seller API；仅生成 listing 信息；采用 API Key 认证方式；新增 Key 管理平台），以下是产品经理视角的结构化需求文档。重点优化了清晰度、可执行性，并去除模糊点。假设产品为 Web 应用（前端：React/Vue；后端：Node.js/Python + 数据库如 MongoDB/PostgreSQL）。
#### 1. 项目概述
- 产品名称：Eldorado AI（或 Eldo AI）
- 产品类型：Web 应用，提供 AI 驱动的 Steal a Brainrot listing 生成服务，针对 Eldorado.gg 卖家。
- 目标用户：Steal a Brainrot 玩家/商人（批量卖家），无需 Eldorado 账号登录，但需使用 API Key 访问工具。
- 核心价值：
	- 上传 Brainrot 截图 → AI 解析 → 生成优化 listing 信息（标题、描述、价格建议），方便用户手动复制/导出到 Eldorado 上架。
	- 通过 API Key 方式，确保安全访问（类似游戏工具密钥，如 Steam Key），避免用户直接登录。
	- 新增 Key 管理平台，供管理员（你/团队）控制用户访问权限。
- 商业目标：简化 listing 准备过程，提高卖家效率；潜在 monetization（如 Key 付费订阅）。
- 关键变更：
	- 暂不集成 Eldorado Seller API（仅生成 listing 数据，不自动上架）。
	- 无用户登录：所有操作基于 API Key 认证。
	- 当前焦点：提供 listing 信息生成 + Key 管理。
#### 2. 认证与访问控制
- 认证方式：API Key 模式（非用户登录）。
	- 用户从你的平台获取/购买 Key（类似游戏激活码）。
	- 每个请求（e.g., 上传截图、生成 listing）需携带 Key 在 header 或 query 中（e.g., Authorization: Bearer ）。
	- Key 生成逻辑：UUID 或随机字符串，支持过期/续费。
- Key 管理平台：独立管理员后台（e.g., /admin/keys），仅你/团队访问（需单独登录或 IP 白名单）。
	- 功能：
		- 生成新 Key：输入用户 ID/邮箱，设置有效期（e.g., 1 月/永久）。
		- 查看 Key 列表：表格显示 Key 值、关联用户、创建时间、上次使用时间、使用次数。
		- 编辑 Key：修改有效期（e.g., 延长/缩短）、封禁/解禁。
		- 监控：日志查看（e.g., Key 使用 IP、时间戳），支持搜索/过滤。
		- 封禁操作：一键禁用 Key（e.g., 因滥用），发送通知给用户。
	- 数据存储：数据库表（keys: id, key_value, user_id, expiry_date, last_used_at, usage_count, status [active/banned]）。
	- 安全：Key 哈希存储；率限（e.g., 每 Key 每天 100 次请求）；过期自动失效。
#### 3. 核心功能流程（User Journey）
1. 用户输入 API Key 访问工具（e.g., 主页需 Key 验证）。
2. 上传 Brainrot 截图。
3. AI 解析图片，生成 listing 信息。
4. 用户编辑/确认，获取最终输出（标题、描述、价格建议等）。
5. 导出 listing 数据（JSON/CSV/文本），用户手动去 Eldorado 上架。
6. 管理员在 Key 管理平台监控/管理。
#### 4. 详细功能需求（按页面/模块）
#### 主页面（Upload & Listing 生成页）
- 访问要求：需有效 API Key（页面加载时验证；无效 Key 显示错误，如 "Invalid or expired key"）。
- 页面标题：Eldorado AI（橙色主题，可自定义）。
- 左侧/图片区：
	- 上传 Brainrot 截图（拖拽/文件选择；支持 PNG/JPG；预览显示）。
- 中央编辑区：
	- Choose title：AI 生成标题（包含 Mutation，如 "Rainbow Mutated Lava Skibidi Toilet (5 Traits) 🌈🔥"；自动加 emoji 基于内置字典）。
	- Free brainrot / No free brainrot：单选（Free 或 Non-free；影响价格建议，Free 默认 $0）。
	- Insert title：手动输入 Brainrot name（如果 AI 未检测）。
	- Insert mutation：手动输入 mutation（optional）。
	- Insert trait amounts：必填 traits 数量（mandatory；AI 初步建议，用户确认）。
	- Check title here：实时预览标题 + 编辑框（"edit here if mistakes"）。
	- Type price：价格输入（默认 AI 建议）。
- 右侧价格参考区：
	- List of prices：显示 Eldorado 同款/相似最低价（e.g., "$600 - seller name (reviews)"；通过爬取公开页面获取，非 API）。
- 底部按钮：
	- Generate Listing：输出完整 listing 信息（标题、描述、价格、类型；格式：文本/JSON/CSV）。
	- Export CSV：生成 Eldorado 兼容 CSV（用于手动 bulk upload）。
#### 设置页面（Settings Page）
- 访问：需 Key 验证。
- 功能：
	- Choose custom description：编辑描述模板（变量如 {name}、{mutation} 等）。
	- Change UI color：颜色选择。
	- Insert custom font：字体选择。
- 保存：用户级设置（关联 Key 存储）。
#### Key 管理平台（Admin Only）
- 独立入口：/admin/keys（需管理员凭证，如用户名/密码）。
- UI：表格 + 表单。
	- Key 列表：列 - Key 值（部分遮掩）、用户、有效期、上次使用时间、使用次数、状态。
	- 操作按钮：生成新 Key、编辑有效期、查看日志、封禁/解禁。
	- 搜索/过滤：按用户/Key/状态。
	- 通知：封禁时可选发邮件/推送。
#### 5. 非功能需求
- AI 解析：使用 OCR（如 EasyOCR/Tesseract）+ 自定义模型；准确率 >80%；fallback 手动。
- Emoji & 模板：内置 mutation/traits 映射；描述默认："High-value {mutation} {name} with {trait_count} traits! {emojis} Price: {price}"。
- 价格参考：爬取 Eldorado 搜索页（e.g., Requests + BeautifulSoup）；缓存 1 小时。
- 安全性：
	- Key 验证：每请求检查（expiry、status、usage limit）。
	- 无用户数据存储（Key 关联匿名 ID）。
	- HTTPS；率限防滥用。
- 性能：解析 <10s；Key 管理响应 <2s。
- 多语言：英文 + 日文（用户在东京）。
- 日志：记录每个 Key 使用（时间、IP、操作）。
#### 6. MVP 范围
- 必须：
	- Key 认证 + 管理平台核心（生成/查看/编辑/封禁）。
	- 截图上传 + AI 解析 + listing 生成（标题/描述/价格参考）。
	- 导出 CSV/文本。
- 后续：
	- Seller API 集成（未来阶段）。
	- 批量截图上传。
	- Key 付费系统。
#### 7. 风险 & 假设
- 风险：Eldorado 页面变更导致爬取失败；Key 滥用。
- 假设：用户手动上架；Key 发放由你控制。

首先 我们不需要真正的对接Seller api 进行一键sell 我们只是出 listing内容 
其次 爬虫抓取的市场数据 将最后再做 等我说 一切都好了 我们只需要爬虫数据了 的时候 我们才会去做 明白了吗
另外很重要的一点 
我已经做好两个前端的页面 设计 我们的前端使用VUE作为技术栈 后端使用python 
前端两个设计不要改我的设计 
另外 后端识别图片的时候使用qwen3-vl-plus模型 
api是OpenAI的协议规范 这是密钥sk-8b222539a757ac8381d0d792c777903d
这是请求地址https://apis.iflow.cn/v1或者https://apis.iflow.cn/v1/chat/completions


